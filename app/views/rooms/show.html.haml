.container 
  - if @messages.present?
    - @messages.each do |m|
      = m.content
  - else
    %p メッセージはまだありません
  = form_with model: @message, url: messages_path, local: true do |f|
    = f.text_field :content, placeholder: 'メッセージを入力'
    = f.hidden_field :room_id, value: @room.id
    %br
    =f.submit '送信'

    
